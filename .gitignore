.venv/
__pycache__/
.pytest_cache/
*.egg-info/
build/
dist/
.DS_Store

# Runtime logs from launchd jobs (per-machine, not VCS).
log/

# Test fixtures — large, copyrighted; excluded from VCS, populated locally.
tests/fixtures/*.pdf
